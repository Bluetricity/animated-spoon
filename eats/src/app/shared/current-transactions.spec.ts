import { CurrentTransactions } from './current-transactions';

describe('CurrentOrders', () => {
  it('should create an instance', () => {
    expect(new CurrentTransactions()).toBeTruthy();
  });
});
